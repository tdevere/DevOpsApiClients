#!/usr/bin/env bash
# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
# ============================================================================
# Deletes a membership between a container and subject.
#
# API:  DELETE {org}/_apis/graph/memberships/{subject_descriptor}/{container_descriptor}?api-version=7.2
# Auth: Basic (PAT)
#
# Usage: SUBJECT_DESCRIPTOR="value" CONTAINER_DESCRIPTOR="value" bash delete_membership.sh
#
# Docs: https://learn.microsoft.com/en-us/rest/api/azure/devops/graph/memberships/remove-membership?view=azure-devops-rest-7.2
# ============================================================================
set -euo pipefail

# ---------------------------------------------------------------------------
# Shared helpers
# ---------------------------------------------------------------------------
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../../_shared/common.sh"

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------
API_VERSION="7.2"
ORG=$(ado_require_env "AZURE_DEVOPS_ORG" "organisation slug")
PAT=$(ado_require_env "AZURE_DEVOPS_PAT" "Personal Access Token")
SUBJECT_DESCRIPTOR=$(ado_require_env "SUBJECT_DESCRIPTOR" "A descriptor to a group or user that is the child subject in the relationship.")
CONTAINER_DESCRIPTOR=$(ado_require_env "CONTAINER_DESCRIPTOR" "A descriptor to a group that is the container in the relationship.")

# ---------------------------------------------------------------------------
# Auth
# ---------------------------------------------------------------------------
AUTH=$(ado_build_auth "$PAT")

# ---------------------------------------------------------------------------
# API call
# ---------------------------------------------------------------------------
URL=$(ado_build_url "$ORG" "_apis/graph/memberships/${SUBJECT_DESCRIPTOR}/${CONTAINER_DESCRIPTOR}" "$API_VERSION" "" "vssps.dev.azure.com")

RESPONSE=$(curl --silent --fail --show-error \
    -X DELETE \
    -H "Authorization: Basic ${AUTH}" \
    -H "Content-Type: application/json" \
    "$URL")

# ---------------------------------------------------------------------------
# Output
# ---------------------------------------------------------------------------
echo "$RESPONSE" | python3 -m json.tool 2>/dev/null || echo "$RESPONSE"
