# AUTO-GENERATED by spec_to_yaml.py — review recommended
domain: Build
resource: Leases
operation: update_leases
ps_verb: Update
ps_noun: Leases
http_method: PATCH
url_path: _apis/build/retention/leases/{lease_id}
api_version: '7.2'
docs_url: https://learn.microsoft.com/en-us/rest/api/azure/devops/build/leases/update?view=azure-devops-rest-7.2
synopsis: Updates the duration or pipeline protection status of a retention lease.
project_scoped: true
params:
- name: lease_id
  env_var: LEASE_ID
  description: The ID of the lease to update.
  required: true
- name: days_valid
  env_var: DAYS_VALID
  description: The number of days to consider the lease valid. A retention lease valid
    for more than 100 years (...
  required: false
  cli_flag: --days-valid
- name: protect_pipeline
  env_var: PROTECT_PIPELINE
  description: If set, this lease will also prevent the pipeline from being deleted
    while the lease is still valid.
  required: false
  cli_flag: --protect-pipeline
body_fields:
- json_path: daysValid
  source: param:days_valid
  description: The number of days to consider the lease valid. A retention lease valid
    for m...
- json_path: protectPipeline
  source: param:protect_pipeline
  description: If set, this lease will also prevent the pipeline from being deleted
    while th...
version_guard_keys:
- createdOn
- definitionId
output_mode: message
fixture_success:
  createdOn: '2026-01-15T10:30:00Z'
  definitionId: 1
  leaseId: 1
  ownerId: sample-string
  protectPipeline: true
  runId: 1
  validUntil: '2026-01-15T10:30:00Z'
fixture_error_404:
  message: Leases not found
  typeKey: LeasesNotFoundException
  errorCode: 0
