#!/usr/bin/env bash
# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
# ============================================================================
# Restore a package version from the recycle bin to its associated feed.
#
# API:  PATCH {org}/_apis/packaging/feeds/{feed_id}/cargo/RecycleBin/packages/{package_name}/versions/{package_version}?api-version=7.2
# Auth: Basic (PAT)
#
# Usage: FEED_ID="value" PACKAGE_NAME="value" PACKAGE_VERSION="value" bash update_restore_package_version_from_recycle_bin.sh
#
# Docs: https://learn.microsoft.com/en-us/rest/api/azure/devops/artifacts-package-types/cargo/restore-package-version-from-recycle-bin?view=azure-devops-rest-7.2
# ============================================================================
set -euo pipefail

# ---------------------------------------------------------------------------
# Shared helpers
# ---------------------------------------------------------------------------
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../../_shared/common.sh"

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------
API_VERSION="7.2"
ORG=$(ado_require_env "AZURE_DEVOPS_ORG" "organisation slug")
PAT=$(ado_require_env "AZURE_DEVOPS_PAT" "Personal Access Token")
PROJECT=$(ado_require_env "PROJECT_ID" "project name or GUID")
FEED_ID=$(ado_require_env "FEED_ID" "Name or ID of the feed.")
PACKAGE_NAME=$(ado_require_env "PACKAGE_NAME" "Name of the package.")
PACKAGE_VERSION=$(ado_require_env "PACKAGE_VERSION" "Version of the package.")

# ---------------------------------------------------------------------------
# Auth
# ---------------------------------------------------------------------------
AUTH=$(ado_build_auth "$PAT")

# ---------------------------------------------------------------------------
# Request body
# ---------------------------------------------------------------------------
BODY=$(cat <<EOF
{
  "deleted": "${DELETED}"
}
EOF
)

# ---------------------------------------------------------------------------
# API call
# ---------------------------------------------------------------------------
URL=$(ado_build_url "$ORG" "_apis/packaging/feeds/${FEED_ID}/cargo/RecycleBin/packages/${PACKAGE_NAME}/versions/${PACKAGE_VERSION}" "$API_VERSION" "$PROJECT" "pkgs.dev.azure.com")

RESPONSE=$(curl --silent --fail --show-error \
    -X PATCH \
    -H "Authorization: Basic ${AUTH}" \
    -H "Content-Type: application/json" \
    -d "$BODY" \
    "$URL")

# ---------------------------------------------------------------------------
# Output
# ---------------------------------------------------------------------------
echo "$RESPONSE" | python3 -m json.tool 2>/dev/null || echo "$RESPONSE"
