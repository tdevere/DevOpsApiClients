# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
<#
.SYNOPSIS
    Restore a package version from the recycle bin to its associated feed.

.DESCRIPTION
    Calls  PATCH {org}/_apis/packaging/feeds/{feed_id}/cargo/RecycleBin/packages/{package_name}/versions/{package_version}?api-version=7.2
    Uses Basic Auth with a PAT stored in $env:AZURE_DEVOPS_PAT.

.LINK
    https://learn.microsoft.com/en-us/rest/api/azure/devops/artifacts-package-types/cargo/restore-package-version-from-recycle-bin?view=azure-devops-rest-7.2
#>

[CmdletBinding()]
param (
    [Parameter(Mandatory)]
    [string]$Deleted,

    [Parameter()]
    [string]$Organization = $env:AZURE_DEVOPS_ORG,

    [Parameter()]
    [string]$ProjectId = $env:PROJECT_ID,

    [Parameter()]
    [string]$FeedId = $env:FEED_ID,

    [Parameter()]
    [string]$PackageName = $env:PACKAGE_NAME,

    [Parameter()]
    [string]$PackageVersion = $env:PACKAGE_VERSION,

    [Parameter()]
    [string]$Pat = $env:AZURE_DEVOPS_PAT
)

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

#--- Shared helpers ---------------------------------------------------------
$_sharedDir = Join-Path (Join-Path (Split-Path $PSScriptRoot -Parent) '..') '_shared'
. (Join-Path $_sharedDir 'AdoAuth.ps1')
. (Join-Path $_sharedDir 'AdoHttp.ps1')

#--- Validate inputs --------------------------------------------------------
Assert-AdoEnv -Name 'Organisation' -Value $Organization
Assert-AdoEnv -Name 'ProjectId' -Value $ProjectId
Assert-AdoEnv -Name 'FeedId' -Value $FeedId
Assert-AdoEnv -Name 'PackageName' -Value $PackageName
Assert-AdoEnv -Name 'PackageVersion' -Value $PackageVersion
Assert-AdoEnv -Name 'PAT' -Value $Pat

#--- API version ------------------------------------------------------------
$ApiVersion = '7.2'

#--- Build auth header ------------------------------------------------------
$headers = New-AdoAuthHeader -Pat $Pat

#--- Build request body -----------------------------------------------------
$body = @{
    deleted = $Deleted
} | ConvertTo-Json -Depth 3

#--- Call the API -----------------------------------------------------------
$uri = New-AdoUrl -Organization $Organization -Path "_apis/packaging/feeds/$FeedId/cargo/RecycleBin/packages/$PackageName/versions/$PackageVersion" -ApiVersion $ApiVersion -Project $ProjectId -BaseHost "pkgs.dev.azure.com"

Write-Verbose "PATCH $uri"

$response = Invoke-RestMethod -Uri $uri -Method Patch -Headers $headers -Body $body -ContentType 'application/json'

#--- Output -----------------------------------------------------------------
$response | ConvertTo-Json -Depth 5
