#!/usr/bin/env bats
# AUTO-GENERATED by _generator — do not edit by hand.
# ============================================================================
# Offline unit tests for get_task_attachments.sh
#
# Uses a stubbed curl function to intercept HTTP calls and return fixture data.
# ============================================================================

SCRIPT_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)"
FIXTURES_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/fixtures" && pwd)"

setup() {
    export AZURE_DEVOPS_ORG="testorg"
    export AZURE_DEVOPS_PAT="fakepat1234567890"
    export PROJECT_ID="a1b2c3d4-e5f6-7890-abcd-ef1234567890"
    export RELEASE_ID="test-value-release_id"
    export ENVIRONMENT_ID="test-value-environment_id"
    export ATTEMPT_ID="test-value-attempt_id"
    export TIMELINE_ID="test-value-timeline_id"
    export TYPE="test-value-type"
    export FIXTURES_DIR
    export BATS_TMPDIR

    STUB_DIR="$(mktemp -d)"
    cat > "$STUB_DIR/curl" << STUBEOF
#!/usr/bin/env bash
echo "\$@" >> "${BATS_TMPDIR}/curl_args.log"
for arg in "\$@"; do
    if [[ "\$arg" == *"/_apis/release"*"api-version="* ]]; then
        cat "${FIXTURES_DIR}/get_task_attachments_200.json"
        exit 0
    fi
done
echo '{"error": "no matching stub"}'
exit 1
STUBEOF
    chmod +x "$STUB_DIR/curl"
    export ORIGINAL_PATH="$PATH"
    export PATH="$STUB_DIR:$PATH"
    export STUB_DIR
    : > "${BATS_TMPDIR}/curl_args.log"
}

teardown() {
    export PATH="$ORIGINAL_PATH"
    rm -rf "$STUB_DIR"
    rm -f "${BATS_TMPDIR}/curl_args.log"
}


# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------

@test "get_task_attachments.sh: exits with error when AZURE_DEVOPS_ORG is unset" {
    unset AZURE_DEVOPS_ORG
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"AZURE_DEVOPS_ORG"* ]]
}

@test "get_task_attachments.sh: exits with error when AZURE_DEVOPS_PAT is unset" {
    unset AZURE_DEVOPS_PAT
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"AZURE_DEVOPS_PAT"* ]]
}

@test "get_task_attachments.sh: exits with error when PROJECT_ID is unset" {
    unset PROJECT_ID
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"PROJECT_ID"* ]]
}

@test "get_task_attachments.sh: exits with error when RELEASE_ID is unset" {
    unset RELEASE_ID
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"RELEASE_ID"* ]]
}

@test "get_task_attachments.sh: exits with error when ENVIRONMENT_ID is unset" {
    unset ENVIRONMENT_ID
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"ENVIRONMENT_ID"* ]]
}

@test "get_task_attachments.sh: exits with error when ATTEMPT_ID is unset" {
    unset ATTEMPT_ID
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"ATTEMPT_ID"* ]]
}

@test "get_task_attachments.sh: exits with error when TIMELINE_ID is unset" {
    unset TIMELINE_ID
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"TIMELINE_ID"* ]]
}

@test "get_task_attachments.sh: exits with error when TYPE is unset" {
    unset TYPE
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"TYPE"* ]]
}

@test "get_task_attachments.sh: succeeds with valid env and stubbed curl" {
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -eq 0 ]
}

@test "get_task_attachments.sh: sends Authorization header" {
    run bash "$SCRIPT_DIR/get_task_attachments.sh"
    [ "$status" -eq 0 ]

    curl_args=$(cat "${BATS_TMPDIR}/curl_args.log")
    [[ "$curl_args" == *"Authorization: Basic"* ]]
}
