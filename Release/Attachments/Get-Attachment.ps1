# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
<#
.SYNOPSIS
    GetTaskAttachments API is deprecated.

.DESCRIPTION
    Calls  GET {org}/_apis/release/releases/{release_id}/environments/{environment_id}/attempts/{attempt_id}/timelines/{timeline_id}/attachments/{type}?api-version=7.2
    Uses Basic Auth with a PAT stored in $env:AZURE_DEVOPS_PAT.

.LINK
    https://learn.microsoft.com/en-us/rest/api/azure/devops/release/attachments/get-task-attachments?view=azure-devops-rest-7.2
#>

[CmdletBinding()]
param (
    [Parameter()]
    [string]$Organization = $env:AZURE_DEVOPS_ORG,

    [Parameter()]
    [string]$ProjectId = $env:PROJECT_ID,

    [Parameter()]
    [string]$ReleaseId = $env:RELEASE_ID,

    [Parameter()]
    [string]$EnvironmentId = $env:ENVIRONMENT_ID,

    [Parameter()]
    [string]$AttemptId = $env:ATTEMPT_ID,

    [Parameter()]
    [string]$TimelineId = $env:TIMELINE_ID,

    [Parameter()]
    [string]$Type = $env:TYPE,

    [Parameter()]
    [string]$Pat = $env:AZURE_DEVOPS_PAT
)

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

#--- Shared helpers ---------------------------------------------------------
$_sharedDir = Join-Path (Join-Path (Split-Path $PSScriptRoot -Parent) '..') '_shared'
. (Join-Path $_sharedDir 'AdoAuth.ps1')
. (Join-Path $_sharedDir 'AdoHttp.ps1')

#--- Validate inputs --------------------------------------------------------
Assert-AdoEnv -Name 'Organisation' -Value $Organization
Assert-AdoEnv -Name 'ProjectId' -Value $ProjectId
Assert-AdoEnv -Name 'ReleaseId' -Value $ReleaseId
Assert-AdoEnv -Name 'EnvironmentId' -Value $EnvironmentId
Assert-AdoEnv -Name 'AttemptId' -Value $AttemptId
Assert-AdoEnv -Name 'TimelineId' -Value $TimelineId
Assert-AdoEnv -Name 'Type' -Value $Type
Assert-AdoEnv -Name 'PAT' -Value $Pat

#--- API version ------------------------------------------------------------
$ApiVersion = '7.2'

#--- Build auth header ------------------------------------------------------
$headers = New-AdoAuthHeader -Pat $Pat

#--- Call the API -----------------------------------------------------------
$uri = New-AdoUrl -Organization $Organization -Path "_apis/release/releases/$ReleaseId/environments/$EnvironmentId/attempts/$AttemptId/timelines/$TimelineId/attachments/$Type" -ApiVersion $ApiVersion -Project $ProjectId -BaseHost "vsrm.dev.azure.com"

Write-Verbose "GET $uri"

$response = Invoke-RestMethod -Uri $uri -Method Get -Headers $headers -ContentType 'application/json'

#--- Output -----------------------------------------------------------------
$response | ConvertTo-Json -Depth 5
