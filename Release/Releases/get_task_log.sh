#!/usr/bin/env bash
# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
# ============================================================================
# Gets the task log of a release as a plain text file.
#
# API:  GET {org}/_apis/release/releases/{release_id}/environments/{environment_id}/deployPhases/{release_deploy_phase_id}/tasks/{task_id}/logs?api-version=7.2
# Auth: Basic (PAT)
#
# Usage: RELEASE_ID="value" ENVIRONMENT_ID="value" RELEASE_DEPLOY_PHASE_ID="value" TASK_ID="value" bash get_task_log.sh
#
# Docs: https://learn.microsoft.com/en-us/rest/api/azure/devops/release/releases/get-task-log?view=azure-devops-rest-7.2
# ============================================================================
set -euo pipefail

# ---------------------------------------------------------------------------
# Shared helpers
# ---------------------------------------------------------------------------
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../../_shared/common.sh"

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------
API_VERSION="7.2"
ORG=$(ado_require_env "AZURE_DEVOPS_ORG" "organisation slug")
PAT=$(ado_require_env "AZURE_DEVOPS_PAT" "Personal Access Token")
PROJECT=$(ado_require_env "PROJECT_ID" "project name or GUID")
RELEASE_ID=$(ado_require_env "RELEASE_ID" "Id of the release.")
ENVIRONMENT_ID=$(ado_require_env "ENVIRONMENT_ID" "Id of release environment.")
RELEASE_DEPLOY_PHASE_ID=$(ado_require_env "RELEASE_DEPLOY_PHASE_ID" "Release deploy phase Id.")
TASK_ID=$(ado_require_env "TASK_ID" "ReleaseTask Id for the log.")

# ---------------------------------------------------------------------------
# Auth
# ---------------------------------------------------------------------------
AUTH=$(ado_build_auth "$PAT")

# ---------------------------------------------------------------------------
# API call
# ---------------------------------------------------------------------------
URL=$(ado_build_url "$ORG" "_apis/release/releases/${RELEASE_ID}/environments/${ENVIRONMENT_ID}/deployPhases/${RELEASE_DEPLOY_PHASE_ID}/tasks/${TASK_ID}/logs" "$API_VERSION" "$PROJECT" "vsrm.dev.azure.com")

RESPONSE=$(curl --silent --fail --show-error \
    -X GET \
    -H "Authorization: Basic ${AUTH}" \
    -H "Content-Type: application/json" \
    "$URL")

# ---------------------------------------------------------------------------
# Output
# ---------------------------------------------------------------------------
echo "$RESPONSE" | python3 -m json.tool 2>/dev/null || echo "$RESPONSE"
