# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
<#
.SYNOPSIS
    Returns a single state definition in a work item type of the process.

.DESCRIPTION
    Calls  GET {org}/_apis/work/processes/{process_id}/workItemTypes/{wit_ref_name}/states/{state_id}?api-version=7.2
    Uses Basic Auth with a PAT stored in $env:AZURE_DEVOPS_PAT.

.LINK
    https://learn.microsoft.com/en-us/rest/api/azure/devops/processes/states/get?view=azure-devops-rest-7.2
#>

[CmdletBinding()]
param (
    [Parameter()]
    [string]$Organization = $env:AZURE_DEVOPS_ORG,

    [Parameter()]
    [string]$ProcessId = $env:PROCESS_ID,

    [Parameter()]
    [string]$WitRefName = $env:WIT_REF_NAME,

    [Parameter()]
    [string]$StateId = $env:STATE_ID,

    [Parameter()]
    [string]$Pat = $env:AZURE_DEVOPS_PAT
)

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

#--- Shared helpers ---------------------------------------------------------
$_sharedDir = Join-Path (Join-Path (Split-Path $PSScriptRoot -Parent) '..') '_shared'
. (Join-Path $_sharedDir 'AdoAuth.ps1')
. (Join-Path $_sharedDir 'AdoHttp.ps1')

#--- Validate inputs --------------------------------------------------------
Assert-AdoEnv -Name 'Organisation' -Value $Organization
Assert-AdoEnv -Name 'ProcessId' -Value $ProcessId
Assert-AdoEnv -Name 'WitRefName' -Value $WitRefName
Assert-AdoEnv -Name 'StateId' -Value $StateId
Assert-AdoEnv -Name 'PAT' -Value $Pat

#--- API version ------------------------------------------------------------
$ApiVersion = '7.2'

#--- Build auth header ------------------------------------------------------
$headers = New-AdoAuthHeader -Pat $Pat

#--- Call the API -----------------------------------------------------------
$uri = New-AdoUrl -Organization $Organization -Path "_apis/work/processes/$ProcessId/workItemTypes/$WitRefName/states/$StateId" -ApiVersion $ApiVersion

Write-Verbose "GET $uri"

$response = Invoke-RestMethod -Uri $uri -Method Get -Headers $headers -ContentType 'application/json'

#--- Version guard ----------------------------------------------------------
if ($response.PSObject.Properties.Name -notcontains 'id') {
    Write-Warning "Unexpected response shape — missing 'id'. The server may not support api-version $ApiVersion."
}
if ($response.PSObject.Properties.Name -notcontains 'name') {
    Write-Warning "Unexpected response shape — missing 'name'. The server may not support api-version $ApiVersion."
}
if ($response.PSObject.Properties.Name -notcontains 'color') {
    Write-Warning "Unexpected response shape — missing 'color'. The server may not support api-version $ApiVersion."
}

#--- Output -----------------------------------------------------------------
Write-Host "States: $($response.name)"
$response | ConvertTo-Json -Depth 5
