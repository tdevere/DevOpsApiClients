#!/usr/bin/env bash
# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
# ============================================================================
# Removes a field in the work item type of the process.
#
# API:  DELETE {org}/_apis/work/processdefinitions/{process_id}/workItemTypes/{wit_ref_name_for_fields}/fields/{field_ref_name}?api-version=4.1
# Auth: Basic (PAT)
#
# Usage: PROCESS_ID="value" WIT_REF_NAME_FOR_FIELDS="value" FIELD_REF_NAME="value" bash delete_field_from_work_item_type.sh
#
# Docs: https://learn.microsoft.com/en-us/rest/api/azure/devops/process-definitions/remove-field-from-work-item-type/remove-field-from-work-item-type?view=azure-devops-rest-7.2
# ============================================================================
set -euo pipefail

# ---------------------------------------------------------------------------
# Shared helpers
# ---------------------------------------------------------------------------
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../../_shared/common.sh"

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------
API_VERSION="4.1"
ORG=$(ado_require_env "AZURE_DEVOPS_ORG" "organisation slug")
PAT=$(ado_require_env "AZURE_DEVOPS_PAT" "Personal Access Token")
PROCESS_ID=$(ado_require_env "PROCESS_ID" "The ID of the process")
WIT_REF_NAME_FOR_FIELDS=$(ado_require_env "WIT_REF_NAME_FOR_FIELDS" "Work item type reference name for fields")
FIELD_REF_NAME=$(ado_require_env "FIELD_REF_NAME" "The reference name of the field")

# ---------------------------------------------------------------------------
# Auth
# ---------------------------------------------------------------------------
AUTH=$(ado_build_auth "$PAT")

# ---------------------------------------------------------------------------
# API call
# ---------------------------------------------------------------------------
URL=$(ado_build_url "$ORG" "_apis/work/processdefinitions/${PROCESS_ID}/workItemTypes/${WIT_REF_NAME_FOR_FIELDS}/fields/${FIELD_REF_NAME}" "$API_VERSION")

RESPONSE=$(curl --silent --fail --show-error \
    -X DELETE \
    -H "Authorization: Basic ${AUTH}" \
    -H "Content-Type: application/json" \
    "$URL")

# ---------------------------------------------------------------------------
# Output
# ---------------------------------------------------------------------------
echo "$RESPONSE" | python3 -m json.tool 2>/dev/null || echo "$RESPONSE"
