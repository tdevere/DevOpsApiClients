#!/usr/bin/env bats
# AUTO-GENERATED by _generator — do not edit by hand.
# ============================================================================
# Offline unit tests for update_pull_request_statuses.sh
#
# Uses a stubbed curl function to intercept HTTP calls and return fixture data.
# ============================================================================

SCRIPT_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)"
FIXTURES_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/fixtures" && pwd)"

setup() {
    export AZURE_DEVOPS_ORG="testorg"
    export AZURE_DEVOPS_PAT="fakepat1234567890"
    export PROJECT_ID="a1b2c3d4-e5f6-7890-abcd-ef1234567890"
    export REPO_ID="test-value-repository_id"
    export PULL_REQUEST_ID="test-value-pull_request_id"
    export FIXTURES_DIR
    export BATS_TMPDIR

    STUB_DIR="$(mktemp -d)"
    cat > "$STUB_DIR/curl" << STUBEOF
#!/usr/bin/env bash
echo "\$@" >> "${BATS_TMPDIR}/curl_args.log"
for arg in "\$@"; do
    if [[ "\$arg" == *"/_apis/git"*"api-version="* ]]; then
        cat "${FIXTURES_DIR}/update_pull_request_statuses_200.json"
        exit 0
    fi
done
echo '{"error": "no matching stub"}'
exit 1
STUBEOF
    chmod +x "$STUB_DIR/curl"
    export ORIGINAL_PATH="$PATH"
    export PATH="$STUB_DIR:$PATH"
    export STUB_DIR
    : > "${BATS_TMPDIR}/curl_args.log"
}

teardown() {
    export PATH="$ORIGINAL_PATH"
    rm -rf "$STUB_DIR"
    rm -f "${BATS_TMPDIR}/curl_args.log"
}


# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------

@test "update_pull_request_statuses.sh: exits with error when AZURE_DEVOPS_ORG is unset" {
    unset AZURE_DEVOPS_ORG
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"AZURE_DEVOPS_ORG"* ]]
}

@test "update_pull_request_statuses.sh: exits with error when AZURE_DEVOPS_PAT is unset" {
    unset AZURE_DEVOPS_PAT
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"AZURE_DEVOPS_PAT"* ]]
}

@test "update_pull_request_statuses.sh: exits with error when PROJECT_ID is unset" {
    unset PROJECT_ID
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"PROJECT_ID"* ]]
}

@test "update_pull_request_statuses.sh: exits with error when REPO_ID is unset" {
    unset REPO_ID
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"REPO_ID"* ]]
}

@test "update_pull_request_statuses.sh: exits with error when PULL_REQUEST_ID is unset" {
    unset PULL_REQUEST_ID
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"PULL_REQUEST_ID"* ]]
}

@test "update_pull_request_statuses.sh: succeeds with valid env and stubbed curl" {
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -eq 0 ]
}

@test "update_pull_request_statuses.sh: sends Authorization header" {
    run bash "$SCRIPT_DIR/update_pull_request_statuses.sh"
    [ "$status" -eq 0 ]

    curl_args=$(cat "${BATS_TMPDIR}/curl_args.log")
    [[ "$curl_args" == *"Authorization: Basic"* ]]
}
