# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
<#
.SYNOPSIS
    Create a new elastic pool.

.DESCRIPTION
    Calls  POST {org}/_apis/distributedtask/elasticpools?api-version=7.2
    Uses Basic Auth with a PAT stored in $env:AZURE_DEVOPS_PAT.

.LINK
    https://learn.microsoft.com/en-us/rest/api/azure/devops/distributed-task/elasticpools/create?view=azure-devops-rest-7.2
#>

[CmdletBinding()]
param (
    [Parameter(Mandatory)]
    [string]$AgentInteractiveUi,

    [Parameter(Mandatory)]
    [string]$AzureId,

    [Parameter(Mandatory)]
    [string]$DesiredIdle,

    [Parameter(Mandatory)]
    [string]$DesiredSize,

    [Parameter(Mandatory)]
    [string]$MaxCapacity,

    [Parameter(Mandatory)]
    [string]$MaxSavedNodeCount,

    [Parameter()]
    [string]$Organization = $env:AZURE_DEVOPS_ORG,

    [Parameter()]
    [string]$PoolName = $env:POOL_NAME,

    [Parameter()]
    [string]$Pat = $env:AZURE_DEVOPS_PAT
)

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

#--- Shared helpers ---------------------------------------------------------
$_sharedDir = Join-Path (Join-Path (Split-Path $PSScriptRoot -Parent) '..') '_shared'
. (Join-Path $_sharedDir 'AdoAuth.ps1')
. (Join-Path $_sharedDir 'AdoHttp.ps1')

#--- Validate inputs --------------------------------------------------------
Assert-AdoEnv -Name 'Organisation' -Value $Organization
Assert-AdoEnv -Name 'PoolName' -Value $PoolName
Assert-AdoEnv -Name 'PAT' -Value $Pat

#--- API version ------------------------------------------------------------
$ApiVersion = '7.2'

#--- Build auth header ------------------------------------------------------
$headers = New-AdoAuthHeader -Pat $Pat

#--- Build request body -----------------------------------------------------
$body = @{
    agentInteractiveUI = $AgentInteractiveUi
    azureId = $AzureId
    desiredIdle = $DesiredIdle
    desiredSize = $DesiredSize
    maxCapacity = $MaxCapacity
    maxSavedNodeCount = $MaxSavedNodeCount
} | ConvertTo-Json -Depth 3

#--- Call the API -----------------------------------------------------------
$uri = New-AdoUrl -Organization $Organization -Path "_apis/distributedtask/elasticpools" -ApiVersion $ApiVersion

Write-Verbose "POST $uri"

$response = Invoke-RestMethod -Uri $uri -Method Post -Headers $headers -Body $body -ContentType 'application/json'

#--- Version guard ----------------------------------------------------------
if ($response.PSObject.Properties.Name -notcontains 'agentPool') {
    Write-Warning "Unexpected response shape — missing 'agentPool'. The server may not support api-version $ApiVersion."
}
if ($response.PSObject.Properties.Name -notcontains 'agentQueue') {
    Write-Warning "Unexpected response shape — missing 'agentQueue'. The server may not support api-version $ApiVersion."
}

#--- Output -----------------------------------------------------------------
$response | ConvertTo-Json -Depth 5
