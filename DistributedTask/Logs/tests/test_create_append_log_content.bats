#!/usr/bin/env bats
# AUTO-GENERATED by _generator — do not edit by hand.
# ============================================================================
# Offline unit tests for create_append_log_content.sh
#
# Uses a stubbed curl function to intercept HTTP calls and return fixture data.
# ============================================================================

SCRIPT_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)"
FIXTURES_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/fixtures" && pwd)"

setup() {
    export AZURE_DEVOPS_ORG="testorg"
    export AZURE_DEVOPS_PAT="fakepat1234567890"
    export SCOPE_IDENTIFIER="test-value-scope_identifier"
    export HUB_NAME="test-value-hub_name"
    export PLAN_ID="test-value-plan_id"
    export LOG_ID="test-value-log_id"
    export FIXTURES_DIR
    export BATS_TMPDIR

    STUB_DIR="$(mktemp -d)"
    cat > "$STUB_DIR/curl" << STUBEOF
#!/usr/bin/env bash
echo "\$@" >> "${BATS_TMPDIR}/curl_args.log"
for arg in "\$@"; do
    if [[ "\$arg" == *"/_apis/distributedtask"*"api-version="* ]]; then
        cat "${FIXTURES_DIR}/create_append_log_content_200.json"
        exit 0
    fi
done
echo '{"error": "no matching stub"}'
exit 1
STUBEOF
    chmod +x "$STUB_DIR/curl"
    export ORIGINAL_PATH="$PATH"
    export PATH="$STUB_DIR:$PATH"
    export STUB_DIR
    : > "${BATS_TMPDIR}/curl_args.log"
}

teardown() {
    export PATH="$ORIGINAL_PATH"
    rm -rf "$STUB_DIR"
    rm -f "${BATS_TMPDIR}/curl_args.log"
}


# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------

@test "create_append_log_content.sh: exits with error when AZURE_DEVOPS_ORG is unset" {
    unset AZURE_DEVOPS_ORG
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"AZURE_DEVOPS_ORG"* ]]
}

@test "create_append_log_content.sh: exits with error when AZURE_DEVOPS_PAT is unset" {
    unset AZURE_DEVOPS_PAT
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"AZURE_DEVOPS_PAT"* ]]
}

@test "create_append_log_content.sh: exits with error when SCOPE_IDENTIFIER is unset" {
    unset SCOPE_IDENTIFIER
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"SCOPE_IDENTIFIER"* ]]
}

@test "create_append_log_content.sh: exits with error when HUB_NAME is unset" {
    unset HUB_NAME
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"HUB_NAME"* ]]
}

@test "create_append_log_content.sh: exits with error when PLAN_ID is unset" {
    unset PLAN_ID
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"PLAN_ID"* ]]
}

@test "create_append_log_content.sh: exits with error when LOG_ID is unset" {
    unset LOG_ID
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -ne 0 ]
    [[ "$output" == *"LOG_ID"* ]]
}

@test "create_append_log_content.sh: succeeds with valid env and stubbed curl" {
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -eq 0 ]
}

@test "create_append_log_content.sh: sends Authorization header" {
    run bash "$SCRIPT_DIR/create_append_log_content.sh"
    [ "$status" -eq 0 ]

    curl_args=$(cat "${BATS_TMPDIR}/curl_args.log")
    [[ "$curl_args" == *"Authorization: Basic"* ]]
}
