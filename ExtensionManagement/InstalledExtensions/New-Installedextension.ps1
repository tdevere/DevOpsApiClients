# AUTO-GENERATED by _generator — do not edit by hand.
# Source of truth: _generator/definitions/
<#
.SYNOPSIS
    Install the specified extension into the account / project collection.

.DESCRIPTION
    Calls  POST {org}/_apis/extensionmanagement/installedextensionsbyname/{publisher_name}/{extension_name}/{version}?api-version=7.2
    Uses Basic Auth with a PAT stored in $env:AZURE_DEVOPS_PAT.

.LINK
    https://learn.microsoft.com/en-us/rest/api/azure/devops/extension-management/installed-extensions/install-extension-by-name?view=azure-devops-rest-7.2
#>

[CmdletBinding()]
param (
    [Parameter()]
    [string]$Organization = $env:AZURE_DEVOPS_ORG,

    [Parameter()]
    [string]$PublisherName = $env:PUBLISHER_NAME,

    [Parameter()]
    [string]$ExtensionName = $env:EXTENSION_NAME,

    [Parameter()]
    [string]$Version = $env:VERSION,

    [Parameter()]
    [string]$Pat = $env:AZURE_DEVOPS_PAT
)

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

#--- Shared helpers ---------------------------------------------------------
$_sharedDir = Join-Path (Join-Path (Split-Path $PSScriptRoot -Parent) '..') '_shared'
. (Join-Path $_sharedDir 'AdoAuth.ps1')
. (Join-Path $_sharedDir 'AdoHttp.ps1')

#--- Validate inputs --------------------------------------------------------
Assert-AdoEnv -Name 'Organisation' -Value $Organization
Assert-AdoEnv -Name 'PublisherName' -Value $PublisherName
Assert-AdoEnv -Name 'ExtensionName' -Value $ExtensionName
Assert-AdoEnv -Name 'Version' -Value $Version
Assert-AdoEnv -Name 'PAT' -Value $Pat

#--- API version ------------------------------------------------------------
$ApiVersion = '7.2'

#--- Build auth header ------------------------------------------------------
$headers = New-AdoAuthHeader -Pat $Pat

#--- Call the API -----------------------------------------------------------
$uri = New-AdoUrl -Organization $Organization -Path "_apis/extensionmanagement/installedextensionsbyname/$PublisherName/$ExtensionName/$Version" -ApiVersion $ApiVersion -BaseHost "extmgmt.dev.azure.com"

Write-Verbose "POST $uri"

$response = Invoke-RestMethod -Uri $uri -Method Post -Headers $headers -ContentType 'application/json'

#--- Version guard ----------------------------------------------------------
if ($response.PSObject.Properties.Name -notcontains 'extensionId') {
    Write-Warning "Unexpected response shape — missing 'extensionId'. The server may not support api-version $ApiVersion."
}
if ($response.PSObject.Properties.Name -notcontains 'extensionName') {
    Write-Warning "Unexpected response shape — missing 'extensionName'. The server may not support api-version $ApiVersion."
}

#--- Output -----------------------------------------------------------------
$response | ConvertTo-Json -Depth 5
